// Local UserRole enum — mirrors Prisma schema.
// Generated Prisma client will export the same enum after `prisma generate`.
export enum UserRole {
  SUPER_ADMIN = 'SUPER_ADMIN',
  GERENTE = 'GERENTE',
  OPERADOR = 'OPERADOR',
  VISUALIZADOR = 'VISUALIZADOR',
}
